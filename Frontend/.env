# Local dev defaults (used automatically if these vars are unset):
#   VITE_API_URL=http://localhost:4002/api
#   VITE_KAIROS_URL=http://localhost:5173
#
# For a deployed Vercel setup, set these to the real project URLs instead:
VITE_API_URL=https://your-ipoms-api.vercel.app/api
VITE_KAIROS_URL=https://your-kairos-frontend.vercel.app
